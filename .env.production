# SkalaUp frontend — production build (served by nginx at https://skalaup.com.br)
# API is reverse-proxied at the same origin under /api, so use a relative base.
VITE_API_URL=/api
VITE_AUTH_REDIRECT_URL=https://skalaup.com.br
